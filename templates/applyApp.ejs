<!doctype html>
<html lang="en">

<head>
	<meta charset="utf-8" />
	<title>Place Order</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
	<h1>New's Article Account</h1>
    <form id="myForm" action="/application" method="post">
		<fieldset>
            <!-- <legend>
                <strong>Customer Information</strong>
            </legend> -->
            
			<Strong>Name: <input type="text" name="name"/></label>
            <br>
            <br>
			<label>Email Address: <input type="email" name="email"></label>
            <br>

            <br><Strong> Age: <input type="text" name="Amo" id="age"/></label><br>

            <br>
                <legend>Describe Status: </legend>
                <label><textarea rows="5" cols="50" name="orderInformation" maxlength="20"></textarea></label>


		</fieldset>
        <br>
        <input type="reset" />
        <input type="submit" value="Submit Application" id="submit"/>
        <hr>
        
        <a href="/">HOME</a>
    </form>
    <script>
        document.getElementById("myForm").addEventListener('submit',function(event){
            const submitButton = document.getElementById("submit");
            const Age = document.getElementById("age").value;
            const confirmed = window.confirm("Are you sure you want to submit your application?\nYou will not be able to change your information once submitted.");
            if (!confirmed) {
                event.preventDefault(); // Prevent form submission if user cancels
                console.log("User canceled the action.");
            }
            else if( Number(Age) < 18){
                event.preventDefault();
                window.alert("Must be 18 years old to sign up")
            }
        });
    </script>
</body>

</html>